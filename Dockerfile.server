FROM node:20-slim
WORKDIR /app

# Copy only the server source — install express directly, no Expo/RN deps
COPY server ./server
RUN npm install express

EXPOSE 3000
ENV PORT=3000

CMD ["node", "server/index.js"]
